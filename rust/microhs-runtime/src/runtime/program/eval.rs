//! Reducer modules: fast stack path, fallback path, strict frames, and dispatch.
use super::*;

mod dispatch;
mod fallback;
mod frames;
mod helpers;
mod persistent;
mod reduce;
mod spine;
mod stack;
